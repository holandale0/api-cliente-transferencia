package com.client.transferencia.infrastructure.mapper;


//import org.mapstruct.Mapper;
//@Mapper(componentModel = "cdi")
public interface TransferenciaMapper {
    //Transferencia map(TransferenciaRequestDTO toMap);
}
